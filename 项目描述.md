# AI-RSS 墨水屏树莓派系统 - 项目描述

## 📋 项目概述

AI-RSS 是一个基于树莓派的墨水屏RSS阅读器系统，采用**前后端分离架构**，结合AI智能摘要技术，为墨水屏硬件提供优化的内容显示解决方案。

### 核心特性
- 📱 **硬件支持**：Waveshare 3.52英寸电子墨水屏（240×360像素）
- 🤖 **AI增强**：支持AI生成的中英文双语摘要
- 🔄 **双服务架构**：内容获取与显示完全分离
- 💾 **本地缓存**：SQLite数据库 + JSON离线备份
- 🎨 **智能排版**：中英文混排自动换行与布局优化
- 🌐 **远程API**：与云端AI-RSS-Hub后端通信
- ⚡ **高可用**：systemd管理，崩溃自动重启

---

## 🏗️ 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AI-RSS-Hub (后端)                              │
│                   远程服务器: 8.134.202.27:8000                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  FastAPI 服务 + SQLite 数据库                                  │  │
│  │  - RSS 抓取调度器 (每小时)                                     │  │
│  │  - AI 摘要生成 (中英文)                                       │  │
│  │  - REST API 端点                                              │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓ HTTP API
┌─────────────────────────────────────────────────────────────────────┐
│                   AI-RSS-Client (前端客户端)                          │
│                         树莓派本地运行                                │
│  ┌───────────────────────────────┐  ┌─────────────────────────────┐│
│  │  ContentFetchService          │  │  DisplayService              ││
│  │  (内容获取服务)               │  │  (显示服务)                   ││
│  │  ──────────────────────────  │  │  ──────────────────────────  ││
│  │  - 每20分钟从后端API获取     │  │  - 每30秒更新墨水屏           ││
│  │  - 本地SQLite缓存            │  │  - 从缓存选择文章             ││
│  │  - JSON离线备份              │  │  - 渲染布局到240×360像素      ││
│  │  - systemd独立进程           │  │  - SPI驱动墨水屏硬件          ││
│  └───────────────────────────────┘  └─────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
                                    ↓ SPI通信
┌─────────────────────────────────────────────────────────────────────┐
│                    Waveshare 3.52" E-Paper                            │
│                      墨水屏显示硬件                                   │
│                    分辨率: 240×360像素                               │
└─────────────────────────────────────────────────────────────────────┘
```

### 架构优势

| 优势 | 说明 |
|------|------|
| **独立运行** | 内容获取和显示完全独立，互不影响 |
| **故障隔离** | 一个服务崩溃不影响另一个服务运行 |
| **灵活管理** | 可单独重启、监控和调试每个服务 |
| **资源优化** | 显示服务无需等待网络请求，响应更快 |
| **离线支持** | 显示服务可在无网络时继续显示缓存内容 |

---

## 📁 项目结构

```
/home/admin/Github/
├── AI-RSS-Hub/              # 后端项目 (远程服务器)
│   ├── app/
│   │   ├── api/
│   │   │   └── routes.py    # REST API端点定义
│   │   ├── main.py          # FastAPI应用入口
│   │   ├── models.py        # 数据模型
│   │   ├── scheduler.py     # RSS抓取调度器
│   │   ├── crud.py          # 数据库操作
│   │   ├── database.py      # 数据库配置
│   │   └── services/        # 业务逻辑服务
│   └── ai_rss_hub.db        # SQLite数据库
│
├── AI-RSS-Client/           # 前端客户端 (树莓派)
│   ├── main.py              # 命令行启动入口
│   ├── config.yml           # 配置文件
│   ├── systemd/             # systemd服务配置
│   │   ├── ai-rss-client-fetch.service    # 内容获取服务
│   │   ├── ai-rss-client-display.service  # 显示服务
│   │   └── manage_service.sh              # 服务管理脚本
│   └── src/
│       ├── services/        # 服务层
│       │   ├── content_fetch_service.py   # 内容获取服务
│       │   ├── display_service.py         # 显示服务
│       │   ├── content_manager.py         # 内容管理器
│       │   └── display_scheduler.py       # 显示调度器
│       ├── fetchers/        # API客户端层
│       │   └── api_client.py          # AI-RSS-Hub API客户端
│       ├── models/          # 数据模型
│       │   ├── article.py             # 文章模型
│       │   └── feed.py                # RSS源模型
│       ├── display/         # 显示相关
│       │   ├── epaper_driver.py      # 墨水屏硬件驱动
│       │   ├── layout_engine.py      # 布局引擎
│       │   ├── renderer.py           # 渲染器
│       │   └── fonts.py              # 字体管理
│       ├── processors/      # 数据处理
│       │   └── cache.py               # 本地缓存
│       └── utils/           # 工具函数
│           └── logger.py             # 日志工具
│
└── epaper-with-raspberrypi/  # 旧版墨水屏项目 (参考)
```

---

## 🔧 核心服务详解

### 1. ContentFetchService (内容获取服务)

**Systemd服务**: `ai-rss-client-fetch.service`

**职责**: 
- 定期从后端API获取新文章
- 更新本地SQLite缓存和JSON备份
- 独立进程运行，可单独重启

**运行参数**:
- **获取间隔**: 20分钟（可配置）
- **单次获取**: 最多200篇文章
- **获取范围**: 近7天文章
- **缓存上限**: 1000篇文章

**启动方式**:
```bash
# 命令行手动运行
python3 main.py fetch-daemon --base-url http://8.134.202.27:8000

# 一次性获取（测试用）
python3 main.py fetch --base-url http://8.134.202.27:8000

# 通过systemd管理
sudo systemctl start ai-rss-client-fetch    # 启动
sudo systemctl stop ai-rss-client-fetch     # 停止
sudo systemctl restart ai-rss-client-fetch  # 重启
sudo systemctl status ai-rss-client-fetch  # 查看状态
```

**配置参数** (`config.yml`):
```yaml
services:
  enabled: true
  interval_minutes: 20              # 获取间隔（分钟）
  max_articles_per_fetch: 200      # 每次最多获取
  fetch_days: 7                     # 获取近N天的文章
  max_cached_articles: 1000        # 本地缓存上限
  incremental_fetch: false         # 增量获取模式
```

---

### 2. DisplayService (显示服务)

**Systemd服务**: `ai-rss-client-display.service`

**职责**:
- 定期从缓存选择文章进行显示
- 渲染布局到240×360像素
- 通过SPI驱动墨水屏更新显示

**运行参数**:
- **更新间隔**: 30秒（可配置）
- **显示天数**: 近7天文章循环
- **显示模式**: 顺序播放/随机播放

**启动方式**:
```bash
# 命令行手动运行
python3 main.py run --base-url http://8.134.202.27:8000

# 自定义间隔（1分钟）
python3 main.py run --interval 1.0 --base-url http://8.134.202.27:8000

# 测试显示硬件
python3 main.py test-display --base-url http://8.134.202.27:8000

# 通过systemd管理
sudo systemctl start ai-rss-client-display    # 启动
sudo systemctl stop ai-rss-client-display     # 停止
sudo systemctl restart ai-rss-client-display  # 重启
sudo systemctl status ai-rss-client-display   # 查看状态
```

**显示流程**:
```
DisplayScheduler
  ↓
1. 从缓存选择文章 (按日期/权重算法)
  ↓
2. LayoutEngine (计算布局，中英文混排)
  ↓
3. Renderer (渲染图像到PIL)
  ↓
4. EPaperDriver (SPI通信)
  ↓
5. 硬件显示更新
```

**布局结构**:
```
┌───────────────────────────┐ 240px
│  Title Area      (35px)    │ ← 标题区 ("AI 资讯")
├───────────────────────────┤
│  ┌─────────────────────┐  │
│  │ 大标题 (Headline)   │  │ ← 文章标题
│  └─────────────────────┘  │
│  ─────────────────        │
│  摘要内容 (Summary)       │ ← AI摘要内容
│  - 自动换行处理            │   (中英文混排)
│  - 字号适配                │
│  - 行距控制                │
│  ...                       │
├───────────────────────────┤
│  Footer         (20px)    │ ← 底部信息
│  来源名称 | 发布日期        │   (如: "科技新闻 | 2024-01-05")
└───────────────────────────┘ 360px
```

**配置参数** (`config.yml`):
```yaml
display_scheduler:
  enabled: true
  interval_minutes: 0.5             # 显示更新间隔（30秒）
  min_display_interval: 30          # 最小显示间隔（秒）
  random_on_empty: true             # 缓存空时随机显示
  mark_as_read_after_display: false # 显示后标记为已读
  display_days: 7                   # 循环播放近N天的文章

display:
  width: 240
  height: 360
  font_size_title: 18
  font_size_headline: 16
  font_size_summary: 15
  font_size_meta: 12
  line_spacing: 1
  margin: 6
```

---

## 🔌 后端API端点

AI-RSS-Hub 提供的REST API端点：

| 端点 | 方法 | 功能 | 认证 |
|------|------|------|------|
| `/api/health` | GET | 健康检查 | 公开 |
| `/api/status` | GET | 系统状态 | 公开 |
| `/api/feeds` | GET | 获取RSS源列表 | 公开 |
| `/api/articles` | GET | 获取文章列表 | 公开 |
| `/api/articles?days=N` | GET | 获取最近N天文章 | 公开 |
| `/api/articles?category=X` | GET | 按分类获取文章 | 公开 |
| `/api/feeds` | POST | 添加RSS源 | 需认证 |
| `/api/feeds/fetch` | POST | 手动触发抓取 | 需认证 |
| `/rss` | GET | RSS订阅输出 | 公开 |

**API基础URL**: `http://8.134.202.27:8000`

**示例**:
```bash
# 健康检查
curl http://8.134.202.27:8000/api/health

# 获取最近3天的文章（最多50篇）
curl http://8.134.202.27:8000/api/articles?days=3&limit=50

# 按分类获取
curl http://8.134.202.27:8000/api/articles?category=tech&limit=20
```

---

## 📊 数据模型

### Article (文章)

```python
{
    "id": int,                    # 文章ID
    "feed_id": int,               # RSS源ID
    "title": str,                 # 标题
    "link": str,                  # 原文链接
    "summary": str,               # AI生成的中文摘要
    "summary_en": str,            # AI生成的英文摘要
    "published_at": str,          # 发布时间 (ISO 8601)
    "created_at": str,            # 创建时间 (ISO 8601)
    "feed_name": str,             # RSS源名称
    "feed_category": str,         # RSS源分类
    "feed_url": str,              # RSS源URL
    # 本地字段（前端维护）
    "displayed_at": str,          # 最后显示时间
    "display_count": int,         # 显示次数
    "is_favorite": bool,          # 是否收藏
    "status": str                 # 显示状态
}
```

### Feed (RSS源)

```python
{
    "id": int,                    # RSS源ID
    "name": str,                  # 源名称
    "url": str,                   # RSS订阅URL
    "category": str,              # 分类 (如 tech, news)
    "is_active": bool,            # 是否活跃
    "created_at": str             # 创建时间
}
```

---

## 🚀 快速开始

### 1. 测试API连接

```bash
cd /home/admin/Github/AI-RSS-Client
python3 main.py test-api --base-url http://8.134.202.27:8000
```

**预期输出**:
```
✓ Health check: ok
✓ System status: running
  - Database: sqlite:///...
  - LLM configured: true
  - Fetch interval: 1 hours
✓ Fetched 5 articles
  - Latest: [文章标题]
✓ API connection test PASSED
```

### 2. 手动获取内容

```bash
python3 main.py fetch --base-url http://8.134.202.27:8000
```

### 3. 测试显示硬件

```bash
python3 main.py test-display --base-url http://8.134.202.27:8000
```

### 4. 启动服务

```bash
# 启动内容获取服务
sudo systemctl start ai-rss-client-fetch
sudo systemctl enable ai-rss-client-fetch

# 启动显示服务
sudo systemctl start ai-rss-client-display
sudo systemctl enable ai-rss-client-display

# 查看状态
sudo systemctl status ai-rss-client-fetch
sudo systemctl status ai-rss-client-display
```

### 5. 查看日志

```bash
# 显示服务日志
sudo journalctl -u ai-rss-client-display -f

# 内容获取服务日志
sudo journalctl -u ai-rss-client-fetch -f

# 查看最近50条
sudo journalctl -u ai-rss-client-display -n 50
```

---

## 📊 当前运行状态

```bash
$ systemctl list-units | grep ai-rss
ai-rss-client-display.service    loaded    active   running    AI-RSS-Client Display Service
ai-rss-client-fetch.service      loaded    active   running    AI-RSS-Client Content Fetch Service
```

✅ 两个服务都正常运行

---

## 🛠️ 常用管理命令

### 命令行工具

```bash
# 测试API连接
python3 main.py test-api --base-url http://8.134.202.27:8000

# 手动获取一次内容
python3 main.py fetch --base-url http://8.134.202.27:8000

# 测试显示硬件
python3 main.py test-display --base-url http://8.134.202.27:8000

# 查看系统状态
python3 main.py status --base-url http://8.134.202.27:8000

# 运行指定周期后退出
python3 main.py run --cycles 5 --base-url http://8.134.202.27:8000
```

### Systemd服务管理

```bash
# 重启服务
sudo systemctl restart ai-rss-client-display
sudo systemctl restart ai-rss-client-fetch

# 同时重启
sudo systemctl restart ai-rss-client-display ai-rss-client-fetch

# 禁用服务
sudo systemctl stop ai-rss-client-display
sudo systemctl disable ai-rss-client-display

# 重新加载配置
sudo systemctl daemon-reload
```

---

## 🔍 故障排查

### 问题1：显示服务不更新

**检查日志**:
```bash
sudo journalctl -u ai-rss-client-display -n 50
```

**可能原因**:
- 缓存中没有新文章
- 硬件初始化失败
- GPIO冲突

**解决方法**:
```bash
# 查看缓存状态
python3 main.py status --base-url http://8.134.202.27:8000

# 测试显示硬件
python3 main.py test-display --base-url http://8.134.202.27:8000

# 手动触发一次显示
python3 main.py run --cycles 5 --base-url http://8.134.202.27:8000
```

### 问题2：内容获取服务不工作

**检查日志**:
```bash
sudo journalctl -u ai-rss-client-fetch -n 50
```

**可能原因**:
- 网络连接问题
- API服务器不可用
- 配置文件错误

**解决方法**:
```bash
# 测试API连接
python3 main.py test-api --base-url http://8.134.202.27:8000

# 手动触发一次获取
python3 main.py fetch --base-url http://8.134.202.27:8000

# 检查网络连接
ping 8.134.202.27
```

---

## 📝 技术栈

| 组件 | 技术 |
|------|------|
| **后端** | FastAPI, SQLModel, SQLite, OpenAI API |
| **前端** | Python 3.11+, requests, Pillow (PIL) |
| **硬件** | 树莓派, Waveshare 3.52" E-Paper |
| **服务管理** | systemd |
| **通信** | HTTP REST API, SPI |

---

## 📄 相关文档

- `SERVICES_ARCHITECTURE.md` - 服务架构详细说明
- `FUNCTIONAL_SPECIFICATION.md` - 功能规格说明
- `config.yml` - 配置文件
- `CLAUDE.md` - 项目开发指南
- `README.md` - 后端项目说明

---

**最后更新**: 2025-06-19
**项目位置**: `/home/admin/Github/AI-RSS-Client`
